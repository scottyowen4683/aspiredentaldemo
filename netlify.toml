# No base. Build the frontend by pointing the command into /frontend.
[build]
command = "npm --prefix frontend ci && npm --prefix frontend run build"
publish = "frontend/dist"

[functions]
directory = "netlify/functions"
node_bundler = "esbuild"
