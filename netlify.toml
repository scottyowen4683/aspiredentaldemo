[build]
  base = "frontend"           # cd into frontend before running command
  command = "npm ci && npm run build"
  publish = "dist"            # dist inside /frontend
  functions = "../netlify/functions"  # functions folder at repo root

# Single-page app routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
