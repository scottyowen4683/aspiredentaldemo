# netlify.toml  — single-app at repo root

[build]
  command = "npm ci && npm run build"
  publish = "dist"
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

# SPA routing (so /admin etc. work)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
